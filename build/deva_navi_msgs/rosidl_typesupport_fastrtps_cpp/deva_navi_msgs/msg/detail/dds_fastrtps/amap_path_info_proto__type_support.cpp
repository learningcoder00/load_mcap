// generated from rosidl_typesupport_fastrtps_cpp/resource/idl__type_support.cpp.em
// with input from deva_navi_msgs:msg/AmapPathInfoProto.idl
// generated code does not contain a copyright notice
#include "deva_navi_msgs/msg/detail/amap_path_info_proto__rosidl_typesupport_fastrtps_cpp.hpp"
#include "deva_navi_msgs/msg/detail/amap_path_info_proto__struct.hpp"

#include <limits>
#include <stdexcept>
#include <string>
#include "rosidl_typesupport_cpp/message_type_support.hpp"
#include "rosidl_typesupport_fastrtps_cpp/identifier.hpp"
#include "rosidl_typesupport_fastrtps_cpp/message_type_support.h"
#include "rosidl_typesupport_fastrtps_cpp/message_type_support_decl.hpp"
#include "rosidl_typesupport_fastrtps_cpp/wstring_conversion.hpp"
#include "fastcdr/Cdr.h"


// forward declaration of message dependencies and their conversion functions
namespace deva_navi_msgs
{
namespace msg
{
namespace typesupport_fastrtps_cpp
{
bool cdr_serialize(
  const deva_navi_msgs::msg::Coord3DDoubleProto &,
  eprosima::fastcdr::Cdr &);
bool cdr_deserialize(
  eprosima::fastcdr::Cdr &,
  deva_navi_msgs::msg::Coord3DDoubleProto &);
size_t get_serialized_size(
  const deva_navi_msgs::msg::Coord3DDoubleProto &,
  size_t current_alignment);
size_t
max_serialized_size_Coord3DDoubleProto(
  bool & full_bounded,
  bool & is_plain,
  size_t current_alignment);
}  // namespace typesupport_fastrtps_cpp
}  // namespace msg
}  // namespace deva_navi_msgs

namespace deva_navi_msgs
{
namespace msg
{
namespace typesupport_fastrtps_cpp
{
bool cdr_serialize(
  const deva_navi_msgs::msg::SegmentInfoProto &,
  eprosima::fastcdr::Cdr &);
bool cdr_deserialize(
  eprosima::fastcdr::Cdr &,
  deva_navi_msgs::msg::SegmentInfoProto &);
size_t get_serialized_size(
  const deva_navi_msgs::msg::SegmentInfoProto &,
  size_t current_alignment);
size_t
max_serialized_size_SegmentInfoProto(
  bool & full_bounded,
  bool & is_plain,
  size_t current_alignment);
}  // namespace typesupport_fastrtps_cpp
}  // namespace msg
}  // namespace deva_navi_msgs

namespace deva_navi_msgs
{
namespace msg
{
namespace typesupport_fastrtps_cpp
{
bool cdr_serialize(
  const deva_navi_msgs::msg::LineIconPointProto &,
  eprosima::fastcdr::Cdr &);
bool cdr_deserialize(
  eprosima::fastcdr::Cdr &,
  deva_navi_msgs::msg::LineIconPointProto &);
size_t get_serialized_size(
  const deva_navi_msgs::msg::LineIconPointProto &,
  size_t current_alignment);
size_t
max_serialized_size_LineIconPointProto(
  bool & full_bounded,
  bool & is_plain,
  size_t current_alignment);
}  // namespace typesupport_fastrtps_cpp
}  // namespace msg
}  // namespace deva_navi_msgs

namespace deva_navi_msgs
{
namespace msg
{
namespace typesupport_fastrtps_cpp
{
bool cdr_serialize(
  const deva_navi_msgs::msg::ViaPointInfoProto &,
  eprosima::fastcdr::Cdr &);
bool cdr_deserialize(
  eprosima::fastcdr::Cdr &,
  deva_navi_msgs::msg::ViaPointInfoProto &);
size_t get_serialized_size(
  const deva_navi_msgs::msg::ViaPointInfoProto &,
  size_t current_alignment);
size_t
max_serialized_size_ViaPointInfoProto(
  bool & full_bounded,
  bool & is_plain,
  size_t current_alignment);
}  // namespace typesupport_fastrtps_cpp
}  // namespace msg
}  // namespace deva_navi_msgs

namespace deva_navi_msgs
{
namespace msg
{
namespace typesupport_fastrtps_cpp
{
bool cdr_serialize(
  const deva_navi_msgs::msg::ChargeStationInfoProto &,
  eprosima::fastcdr::Cdr &);
bool cdr_deserialize(
  eprosima::fastcdr::Cdr &,
  deva_navi_msgs::msg::ChargeStationInfoProto &);
size_t get_serialized_size(
  const deva_navi_msgs::msg::ChargeStationInfoProto &,
  size_t current_alignment);
size_t
max_serialized_size_ChargeStationInfoProto(
  bool & full_bounded,
  bool & is_plain,
  size_t current_alignment);
}  // namespace typesupport_fastrtps_cpp
}  // namespace msg
}  // namespace deva_navi_msgs

namespace deva_navi_msgs
{
namespace msg
{
namespace typesupport_fastrtps_cpp
{
bool cdr_serialize(
  const deva_navi_msgs::msg::OddSegInfoProto &,
  eprosima::fastcdr::Cdr &);
bool cdr_deserialize(
  eprosima::fastcdr::Cdr &,
  deva_navi_msgs::msg::OddSegInfoProto &);
size_t get_serialized_size(
  const deva_navi_msgs::msg::OddSegInfoProto &,
  size_t current_alignment);
size_t
max_serialized_size_OddSegInfoProto(
  bool & full_bounded,
  bool & is_plain,
  size_t current_alignment);
}  // namespace typesupport_fastrtps_cpp
}  // namespace msg
}  // namespace deva_navi_msgs

namespace deva_navi_msgs
{
namespace msg
{
namespace typesupport_fastrtps_cpp
{
bool cdr_serialize(
  const deva_navi_msgs::msg::GroupSegmentProto &,
  eprosima::fastcdr::Cdr &);
bool cdr_deserialize(
  eprosima::fastcdr::Cdr &,
  deva_navi_msgs::msg::GroupSegmentProto &);
size_t get_serialized_size(
  const deva_navi_msgs::msg::GroupSegmentProto &,
  size_t current_alignment);
size_t
max_serialized_size_GroupSegmentProto(
  bool & full_bounded,
  bool & is_plain,
  size_t current_alignment);
}  // namespace typesupport_fastrtps_cpp
}  // namespace msg
}  // namespace deva_navi_msgs

namespace deva_navi_msgs
{
namespace msg
{
namespace typesupport_fastrtps_cpp
{
bool cdr_serialize(
  const deva_navi_msgs::msg::RestrictionInfoProto &,
  eprosima::fastcdr::Cdr &);
bool cdr_deserialize(
  eprosima::fastcdr::Cdr &,
  deva_navi_msgs::msg::RestrictionInfoProto &);
size_t get_serialized_size(
  const deva_navi_msgs::msg::RestrictionInfoProto &,
  size_t current_alignment);
size_t
max_serialized_size_RestrictionInfoProto(
  bool & full_bounded,
  bool & is_plain,
  size_t current_alignment);
}  // namespace typesupport_fastrtps_cpp
}  // namespace msg
}  // namespace deva_navi_msgs

namespace deva_navi_msgs
{
namespace msg
{
namespace typesupport_fastrtps_cpp
{
bool cdr_serialize(
  const deva_navi_msgs::msg::POIInfoProto &,
  eprosima::fastcdr::Cdr &);
bool cdr_deserialize(
  eprosima::fastcdr::Cdr &,
  deva_navi_msgs::msg::POIInfoProto &);
size_t get_serialized_size(
  const deva_navi_msgs::msg::POIInfoProto &,
  size_t current_alignment);
size_t
max_serialized_size_POIInfoProto(
  bool & full_bounded,
  bool & is_plain,
  size_t current_alignment);
}  // namespace typesupport_fastrtps_cpp
}  // namespace msg
}  // namespace deva_navi_msgs

namespace deva_navi_msgs
{
namespace msg
{
namespace typesupport_fastrtps_cpp
{
bool cdr_serialize(
  const deva_navi_msgs::msg::TipInfoProto &,
  eprosima::fastcdr::Cdr &);
bool cdr_deserialize(
  eprosima::fastcdr::Cdr &,
  deva_navi_msgs::msg::TipInfoProto &);
size_t get_serialized_size(
  const deva_navi_msgs::msg::TipInfoProto &,
  size_t current_alignment);
size_t
max_serialized_size_TipInfoProto(
  bool & full_bounded,
  bool & is_plain,
  size_t current_alignment);
}  // namespace typesupport_fastrtps_cpp
}  // namespace msg
}  // namespace deva_navi_msgs

namespace deva_navi_msgs
{
namespace msg
{
namespace typesupport_fastrtps_cpp
{
bool cdr_serialize(
  const deva_navi_msgs::msg::ElecPathInfoProto &,
  eprosima::fastcdr::Cdr &);
bool cdr_deserialize(
  eprosima::fastcdr::Cdr &,
  deva_navi_msgs::msg::ElecPathInfoProto &);
size_t get_serialized_size(
  const deva_navi_msgs::msg::ElecPathInfoProto &,
  size_t current_alignment);
size_t
max_serialized_size_ElecPathInfoProto(
  bool & full_bounded,
  bool & is_plain,
  size_t current_alignment);
}  // namespace typesupport_fastrtps_cpp
}  // namespace msg
}  // namespace deva_navi_msgs

namespace deva_navi_msgs
{
namespace msg
{
namespace typesupport_fastrtps_cpp
{
bool cdr_serialize(
  const deva_navi_msgs::msg::PathDescriptionInfoProto &,
  eprosima::fastcdr::Cdr &);
bool cdr_deserialize(
  eprosima::fastcdr::Cdr &,
  deva_navi_msgs::msg::PathDescriptionInfoProto &);
size_t get_serialized_size(
  const deva_navi_msgs::msg::PathDescriptionInfoProto &,
  size_t current_alignment);
size_t
max_serialized_size_PathDescriptionInfoProto(
  bool & full_bounded,
  bool & is_plain,
  size_t current_alignment);
}  // namespace typesupport_fastrtps_cpp
}  // namespace msg
}  // namespace deva_navi_msgs


namespace deva_navi_msgs
{

namespace msg
{

namespace typesupport_fastrtps_cpp
{

bool
ROSIDL_TYPESUPPORT_FASTRTPS_CPP_PUBLIC_deva_navi_msgs
cdr_serialize(
  const deva_navi_msgs::msg::AmapPathInfoProto & ros_message,
  eprosima::fastcdr::Cdr & cdr)
{
  // Member: all_3d_traffic_lights
  {
    size_t size = ros_message.all_3d_traffic_lights.size();
    cdr << static_cast<uint32_t>(size);
    for (size_t i = 0; i < size; i++) {
      deva_navi_msgs::msg::typesupport_fastrtps_cpp::cdr_serialize(
        ros_message.all_3d_traffic_lights[i],
        cdr);
    }
  }
  // Member: segment_infos
  {
    size_t size = ros_message.segment_infos.size();
    cdr << static_cast<uint32_t>(size);
    for (size_t i = 0; i < size; i++) {
      deva_navi_msgs::msg::typesupport_fastrtps_cpp::cdr_serialize(
        ros_message.segment_infos[i],
        cdr);
    }
  }
  // Member: line_icon_points
  {
    size_t size = ros_message.line_icon_points.size();
    cdr << static_cast<uint32_t>(size);
    for (size_t i = 0; i < size; i++) {
      deva_navi_msgs::msg::typesupport_fastrtps_cpp::cdr_serialize(
        ros_message.line_icon_points[i],
        cdr);
    }
  }
  // Member: city_adcode_list
  {
    cdr << ros_message.city_adcode_list;
  }
  // Member: via_point_info
  {
    size_t size = ros_message.via_point_info.size();
    cdr << static_cast<uint32_t>(size);
    for (size_t i = 0; i < size; i++) {
      deva_navi_msgs::msg::typesupport_fastrtps_cpp::cdr_serialize(
        ros_message.via_point_info[i],
        cdr);
    }
  }
  // Member: charge_station_info
  {
    size_t size = ros_message.charge_station_info.size();
    cdr << static_cast<uint32_t>(size);
    for (size_t i = 0; i < size; i++) {
      deva_navi_msgs::msg::typesupport_fastrtps_cpp::cdr_serialize(
        ros_message.charge_station_info[i],
        cdr);
    }
  }
  // Member: odd_seg_infos
  {
    size_t size = ros_message.odd_seg_infos.size();
    cdr << static_cast<uint32_t>(size);
    for (size_t i = 0; i < size; i++) {
      deva_navi_msgs::msg::typesupport_fastrtps_cpp::cdr_serialize(
        ros_message.odd_seg_infos[i],
        cdr);
    }
  }
  // Member: plan_channel_id
  cdr << ros_message.plan_channel_id;
  // Member: offline_req_custom_identity_id
  cdr << ros_message.offline_req_custom_identity_id;
  // Member: is_valid
  cdr << (ros_message.is_valid ? true : false);
  // Member: path_id
  cdr << ros_message.path_id;
  // Member: type
  cdr << ros_message.type;
  // Member: length
  cdr << ros_message.length;
  // Member: strategy
  cdr << ros_message.strategy;
  // Member: travel_time
  cdr << ros_message.travel_time;
  // Member: static_travel_time
  cdr << ros_message.static_travel_time;
  // Member: toll_cost
  cdr << ros_message.toll_cost;
  // Member: navi_id
  cdr << ros_message.navi_id;
  // Member: is_online
  cdr << (ros_message.is_online ? true : false);
  // Member: group_segments
  {
    size_t size = ros_message.group_segments.size();
    cdr << static_cast<uint32_t>(size);
    for (size_t i = 0; i < size; i++) {
      deva_navi_msgs::msg::typesupport_fastrtps_cpp::cdr_serialize(
        ros_message.group_segments[i],
        cdr);
    }
  }
  // Member: restriction_info
  deva_navi_msgs::msg::typesupport_fastrtps_cpp::cdr_serialize(
    ros_message.restriction_info,
    cdr);
  // Member: end_poi
  deva_navi_msgs::msg::typesupport_fastrtps_cpp::cdr_serialize(
    ros_message.end_poi,
    cdr);
  // Member: tip
  deva_navi_msgs::msg::typesupport_fastrtps_cpp::cdr_serialize(
    ros_message.tip,
    cdr);
  // Member: end_direction
  cdr << ros_message.end_direction;
  // Member: is_holiday_free
  cdr << (ros_message.is_holiday_free ? true : false);
  // Member: is_truck_path
  cdr << (ros_message.is_truck_path ? true : false);
  // Member: data_version
  cdr << ros_message.data_version;
  // Member: normal_plan_time
  cdr << ros_message.normal_plan_time;
  // Member: route_type
  cdr << ros_message.route_type;
  // Member: elec_path_info
  deva_navi_msgs::msg::typesupport_fastrtps_cpp::cdr_serialize(
    ros_message.elec_path_info,
    cdr);
  // Member: path_description_info
  deva_navi_msgs::msg::typesupport_fastrtps_cpp::cdr_serialize(
    ros_message.path_description_info,
    cdr);
  // Member: remain_toll_cost
  cdr << ros_message.remain_toll_cost;
  // Member: main_route_remain_toll_cost
  cdr << ros_message.main_route_remain_toll_cost;
  return true;
}

bool
ROSIDL_TYPESUPPORT_FASTRTPS_CPP_PUBLIC_deva_navi_msgs
cdr_deserialize(
  eprosima::fastcdr::Cdr & cdr,
  deva_navi_msgs::msg::AmapPathInfoProto & ros_message)
{
  // Member: all_3d_traffic_lights
  {
    uint32_t cdrSize;
    cdr >> cdrSize;
    size_t size = static_cast<size_t>(cdrSize);

    // Check there are at least 'size' remaining bytes in the CDR stream before resizing
    auto old_state = cdr.getState();
    bool correct_size = cdr.jump(size);
    cdr.setState(old_state);
    if (!correct_size) {
      fprintf(stderr, "sequence size exceeds remaining buffer\n");
      return false;
    }

    ros_message.all_3d_traffic_lights.resize(size);
    for (size_t i = 0; i < size; i++) {
      deva_navi_msgs::msg::typesupport_fastrtps_cpp::cdr_deserialize(
        cdr, ros_message.all_3d_traffic_lights[i]);
    }
  }

  // Member: segment_infos
  {
    uint32_t cdrSize;
    cdr >> cdrSize;
    size_t size = static_cast<size_t>(cdrSize);

    // Check there are at least 'size' remaining bytes in the CDR stream before resizing
    auto old_state = cdr.getState();
    bool correct_size = cdr.jump(size);
    cdr.setState(old_state);
    if (!correct_size) {
      fprintf(stderr, "sequence size exceeds remaining buffer\n");
      return false;
    }

    ros_message.segment_infos.resize(size);
    for (size_t i = 0; i < size; i++) {
      deva_navi_msgs::msg::typesupport_fastrtps_cpp::cdr_deserialize(
        cdr, ros_message.segment_infos[i]);
    }
  }

  // Member: line_icon_points
  {
    uint32_t cdrSize;
    cdr >> cdrSize;
    size_t size = static_cast<size_t>(cdrSize);

    // Check there are at least 'size' remaining bytes in the CDR stream before resizing
    auto old_state = cdr.getState();
    bool correct_size = cdr.jump(size);
    cdr.setState(old_state);
    if (!correct_size) {
      fprintf(stderr, "sequence size exceeds remaining buffer\n");
      return false;
    }

    ros_message.line_icon_points.resize(size);
    for (size_t i = 0; i < size; i++) {
      deva_navi_msgs::msg::typesupport_fastrtps_cpp::cdr_deserialize(
        cdr, ros_message.line_icon_points[i]);
    }
  }

  // Member: city_adcode_list
  {
    cdr >> ros_message.city_adcode_list;
  }

  // Member: via_point_info
  {
    uint32_t cdrSize;
    cdr >> cdrSize;
    size_t size = static_cast<size_t>(cdrSize);

    // Check there are at least 'size' remaining bytes in the CDR stream before resizing
    auto old_state = cdr.getState();
    bool correct_size = cdr.jump(size);
    cdr.setState(old_state);
    if (!correct_size) {
      fprintf(stderr, "sequence size exceeds remaining buffer\n");
      return false;
    }

    ros_message.via_point_info.resize(size);
    for (size_t i = 0; i < size; i++) {
      deva_navi_msgs::msg::typesupport_fastrtps_cpp::cdr_deserialize(
        cdr, ros_message.via_point_info[i]);
    }
  }

  // Member: charge_station_info
  {
    uint32_t cdrSize;
    cdr >> cdrSize;
    size_t size = static_cast<size_t>(cdrSize);

    // Check there are at least 'size' remaining bytes in the CDR stream before resizing
    auto old_state = cdr.getState();
    bool correct_size = cdr.jump(size);
    cdr.setState(old_state);
    if (!correct_size) {
      fprintf(stderr, "sequence size exceeds remaining buffer\n");
      return false;
    }

    ros_message.charge_station_info.resize(size);
    for (size_t i = 0; i < size; i++) {
      deva_navi_msgs::msg::typesupport_fastrtps_cpp::cdr_deserialize(
        cdr, ros_message.charge_station_info[i]);
    }
  }

  // Member: odd_seg_infos
  {
    uint32_t cdrSize;
    cdr >> cdrSize;
    size_t size = static_cast<size_t>(cdrSize);

    // Check there are at least 'size' remaining bytes in the CDR stream before resizing
    auto old_state = cdr.getState();
    bool correct_size = cdr.jump(size);
    cdr.setState(old_state);
    if (!correct_size) {
      fprintf(stderr, "sequence size exceeds remaining buffer\n");
      return false;
    }

    ros_message.odd_seg_infos.resize(size);
    for (size_t i = 0; i < size; i++) {
      deva_navi_msgs::msg::typesupport_fastrtps_cpp::cdr_deserialize(
        cdr, ros_message.odd_seg_infos[i]);
    }
  }

  // Member: plan_channel_id
  cdr >> ros_message.plan_channel_id;

  // Member: offline_req_custom_identity_id
  cdr >> ros_message.offline_req_custom_identity_id;

  // Member: is_valid
  {
    uint8_t tmp;
    cdr >> tmp;
    ros_message.is_valid = tmp ? true : false;
  }

  // Member: path_id
  cdr >> ros_message.path_id;

  // Member: type
  cdr >> ros_message.type;

  // Member: length
  cdr >> ros_message.length;

  // Member: strategy
  cdr >> ros_message.strategy;

  // Member: travel_time
  cdr >> ros_message.travel_time;

  // Member: static_travel_time
  cdr >> ros_message.static_travel_time;

  // Member: toll_cost
  cdr >> ros_message.toll_cost;

  // Member: navi_id
  cdr >> ros_message.navi_id;

  // Member: is_online
  {
    uint8_t tmp;
    cdr >> tmp;
    ros_message.is_online = tmp ? true : false;
  }

  // Member: group_segments
  {
    uint32_t cdrSize;
    cdr >> cdrSize;
    size_t size = static_cast<size_t>(cdrSize);

    // Check there are at least 'size' remaining bytes in the CDR stream before resizing
    auto old_state = cdr.getState();
    bool correct_size = cdr.jump(size);
    cdr.setState(old_state);
    if (!correct_size) {
      fprintf(stderr, "sequence size exceeds remaining buffer\n");
      return false;
    }

    ros_message.group_segments.resize(size);
    for (size_t i = 0; i < size; i++) {
      deva_navi_msgs::msg::typesupport_fastrtps_cpp::cdr_deserialize(
        cdr, ros_message.group_segments[i]);
    }
  }

  // Member: restriction_info
  deva_navi_msgs::msg::typesupport_fastrtps_cpp::cdr_deserialize(
    cdr, ros_message.restriction_info);

  // Member: end_poi
  deva_navi_msgs::msg::typesupport_fastrtps_cpp::cdr_deserialize(
    cdr, ros_message.end_poi);

  // Member: tip
  deva_navi_msgs::msg::typesupport_fastrtps_cpp::cdr_deserialize(
    cdr, ros_message.tip);

  // Member: end_direction
  cdr >> ros_message.end_direction;

  // Member: is_holiday_free
  {
    uint8_t tmp;
    cdr >> tmp;
    ros_message.is_holiday_free = tmp ? true : false;
  }

  // Member: is_truck_path
  {
    uint8_t tmp;
    cdr >> tmp;
    ros_message.is_truck_path = tmp ? true : false;
  }

  // Member: data_version
  cdr >> ros_message.data_version;

  // Member: normal_plan_time
  cdr >> ros_message.normal_plan_time;

  // Member: route_type
  cdr >> ros_message.route_type;

  // Member: elec_path_info
  deva_navi_msgs::msg::typesupport_fastrtps_cpp::cdr_deserialize(
    cdr, ros_message.elec_path_info);

  // Member: path_description_info
  deva_navi_msgs::msg::typesupport_fastrtps_cpp::cdr_deserialize(
    cdr, ros_message.path_description_info);

  // Member: remain_toll_cost
  cdr >> ros_message.remain_toll_cost;

  // Member: main_route_remain_toll_cost
  cdr >> ros_message.main_route_remain_toll_cost;

  return true;
}  // NOLINT(readability/fn_size)

size_t
ROSIDL_TYPESUPPORT_FASTRTPS_CPP_PUBLIC_deva_navi_msgs
get_serialized_size(
  const deva_navi_msgs::msg::AmapPathInfoProto & ros_message,
  size_t current_alignment)
{
  size_t initial_alignment = current_alignment;

  const size_t padding = 4;
  const size_t wchar_size = 4;
  (void)padding;
  (void)wchar_size;

  // Member: all_3d_traffic_lights
  {
    size_t array_size = ros_message.all_3d_traffic_lights.size();

    current_alignment += padding +
      eprosima::fastcdr::Cdr::alignment(current_alignment, padding);

    for (size_t index = 0; index < array_size; ++index) {
      current_alignment +=
        deva_navi_msgs::msg::typesupport_fastrtps_cpp::get_serialized_size(
        ros_message.all_3d_traffic_lights[index], current_alignment);
    }
  }
  // Member: segment_infos
  {
    size_t array_size = ros_message.segment_infos.size();

    current_alignment += padding +
      eprosima::fastcdr::Cdr::alignment(current_alignment, padding);

    for (size_t index = 0; index < array_size; ++index) {
      current_alignment +=
        deva_navi_msgs::msg::typesupport_fastrtps_cpp::get_serialized_size(
        ros_message.segment_infos[index], current_alignment);
    }
  }
  // Member: line_icon_points
  {
    size_t array_size = ros_message.line_icon_points.size();

    current_alignment += padding +
      eprosima::fastcdr::Cdr::alignment(current_alignment, padding);

    for (size_t index = 0; index < array_size; ++index) {
      current_alignment +=
        deva_navi_msgs::msg::typesupport_fastrtps_cpp::get_serialized_size(
        ros_message.line_icon_points[index], current_alignment);
    }
  }
  // Member: city_adcode_list
  {
    size_t array_size = ros_message.city_adcode_list.size();

    current_alignment += padding +
      eprosima::fastcdr::Cdr::alignment(current_alignment, padding);
    size_t item_size = sizeof(ros_message.city_adcode_list[0]);
    current_alignment += array_size * item_size +
      eprosima::fastcdr::Cdr::alignment(current_alignment, item_size);
  }
  // Member: via_point_info
  {
    size_t array_size = ros_message.via_point_info.size();

    current_alignment += padding +
      eprosima::fastcdr::Cdr::alignment(current_alignment, padding);

    for (size_t index = 0; index < array_size; ++index) {
      current_alignment +=
        deva_navi_msgs::msg::typesupport_fastrtps_cpp::get_serialized_size(
        ros_message.via_point_info[index], current_alignment);
    }
  }
  // Member: charge_station_info
  {
    size_t array_size = ros_message.charge_station_info.size();

    current_alignment += padding +
      eprosima::fastcdr::Cdr::alignment(current_alignment, padding);

    for (size_t index = 0; index < array_size; ++index) {
      current_alignment +=
        deva_navi_msgs::msg::typesupport_fastrtps_cpp::get_serialized_size(
        ros_message.charge_station_info[index], current_alignment);
    }
  }
  // Member: odd_seg_infos
  {
    size_t array_size = ros_message.odd_seg_infos.size();

    current_alignment += padding +
      eprosima::fastcdr::Cdr::alignment(current_alignment, padding);

    for (size_t index = 0; index < array_size; ++index) {
      current_alignment +=
        deva_navi_msgs::msg::typesupport_fastrtps_cpp::get_serialized_size(
        ros_message.odd_seg_infos[index], current_alignment);
    }
  }
  // Member: plan_channel_id
  current_alignment += padding +
    eprosima::fastcdr::Cdr::alignment(current_alignment, padding) +
    (ros_message.plan_channel_id.size() + 1);
  // Member: offline_req_custom_identity_id
  current_alignment += padding +
    eprosima::fastcdr::Cdr::alignment(current_alignment, padding) +
    (ros_message.offline_req_custom_identity_id.size() + 1);
  // Member: is_valid
  {
    size_t item_size = sizeof(ros_message.is_valid);
    current_alignment += item_size +
      eprosima::fastcdr::Cdr::alignment(current_alignment, item_size);
  }
  // Member: path_id
  {
    size_t item_size = sizeof(ros_message.path_id);
    current_alignment += item_size +
      eprosima::fastcdr::Cdr::alignment(current_alignment, item_size);
  }
  // Member: type
  {
    size_t item_size = sizeof(ros_message.type);
    current_alignment += item_size +
      eprosima::fastcdr::Cdr::alignment(current_alignment, item_size);
  }
  // Member: length
  {
    size_t item_size = sizeof(ros_message.length);
    current_alignment += item_size +
      eprosima::fastcdr::Cdr::alignment(current_alignment, item_size);
  }
  // Member: strategy
  {
    size_t item_size = sizeof(ros_message.strategy);
    current_alignment += item_size +
      eprosima::fastcdr::Cdr::alignment(current_alignment, item_size);
  }
  // Member: travel_time
  {
    size_t item_size = sizeof(ros_message.travel_time);
    current_alignment += item_size +
      eprosima::fastcdr::Cdr::alignment(current_alignment, item_size);
  }
  // Member: static_travel_time
  {
    size_t item_size = sizeof(ros_message.static_travel_time);
    current_alignment += item_size +
      eprosima::fastcdr::Cdr::alignment(current_alignment, item_size);
  }
  // Member: toll_cost
  {
    size_t item_size = sizeof(ros_message.toll_cost);
    current_alignment += item_size +
      eprosima::fastcdr::Cdr::alignment(current_alignment, item_size);
  }
  // Member: navi_id
  current_alignment += padding +
    eprosima::fastcdr::Cdr::alignment(current_alignment, padding) +
    (ros_message.navi_id.size() + 1);
  // Member: is_online
  {
    size_t item_size = sizeof(ros_message.is_online);
    current_alignment += item_size +
      eprosima::fastcdr::Cdr::alignment(current_alignment, item_size);
  }
  // Member: group_segments
  {
    size_t array_size = ros_message.group_segments.size();

    current_alignment += padding +
      eprosima::fastcdr::Cdr::alignment(current_alignment, padding);

    for (size_t index = 0; index < array_size; ++index) {
      current_alignment +=
        deva_navi_msgs::msg::typesupport_fastrtps_cpp::get_serialized_size(
        ros_message.group_segments[index], current_alignment);
    }
  }
  // Member: restriction_info

  current_alignment +=
    deva_navi_msgs::msg::typesupport_fastrtps_cpp::get_serialized_size(
    ros_message.restriction_info, current_alignment);
  // Member: end_poi

  current_alignment +=
    deva_navi_msgs::msg::typesupport_fastrtps_cpp::get_serialized_size(
    ros_message.end_poi, current_alignment);
  // Member: tip

  current_alignment +=
    deva_navi_msgs::msg::typesupport_fastrtps_cpp::get_serialized_size(
    ros_message.tip, current_alignment);
  // Member: end_direction
  {
    size_t item_size = sizeof(ros_message.end_direction);
    current_alignment += item_size +
      eprosima::fastcdr::Cdr::alignment(current_alignment, item_size);
  }
  // Member: is_holiday_free
  {
    size_t item_size = sizeof(ros_message.is_holiday_free);
    current_alignment += item_size +
      eprosima::fastcdr::Cdr::alignment(current_alignment, item_size);
  }
  // Member: is_truck_path
  {
    size_t item_size = sizeof(ros_message.is_truck_path);
    current_alignment += item_size +
      eprosima::fastcdr::Cdr::alignment(current_alignment, item_size);
  }
  // Member: data_version
  {
    size_t item_size = sizeof(ros_message.data_version);
    current_alignment += item_size +
      eprosima::fastcdr::Cdr::alignment(current_alignment, item_size);
  }
  // Member: normal_plan_time
  {
    size_t item_size = sizeof(ros_message.normal_plan_time);
    current_alignment += item_size +
      eprosima::fastcdr::Cdr::alignment(current_alignment, item_size);
  }
  // Member: route_type
  {
    size_t item_size = sizeof(ros_message.route_type);
    current_alignment += item_size +
      eprosima::fastcdr::Cdr::alignment(current_alignment, item_size);
  }
  // Member: elec_path_info

  current_alignment +=
    deva_navi_msgs::msg::typesupport_fastrtps_cpp::get_serialized_size(
    ros_message.elec_path_info, current_alignment);
  // Member: path_description_info

  current_alignment +=
    deva_navi_msgs::msg::typesupport_fastrtps_cpp::get_serialized_size(
    ros_message.path_description_info, current_alignment);
  // Member: remain_toll_cost
  {
    size_t item_size = sizeof(ros_message.remain_toll_cost);
    current_alignment += item_size +
      eprosima::fastcdr::Cdr::alignment(current_alignment, item_size);
  }
  // Member: main_route_remain_toll_cost
  {
    size_t item_size = sizeof(ros_message.main_route_remain_toll_cost);
    current_alignment += item_size +
      eprosima::fastcdr::Cdr::alignment(current_alignment, item_size);
  }

  return current_alignment - initial_alignment;
}

size_t
ROSIDL_TYPESUPPORT_FASTRTPS_CPP_PUBLIC_deva_navi_msgs
max_serialized_size_AmapPathInfoProto(
  bool & full_bounded,
  bool & is_plain,
  size_t current_alignment)
{
  size_t initial_alignment = current_alignment;

  const size_t padding = 4;
  const size_t wchar_size = 4;
  size_t last_member_size = 0;
  (void)last_member_size;
  (void)padding;
  (void)wchar_size;

  full_bounded = true;
  is_plain = true;


  // Member: all_3d_traffic_lights
  {
    size_t array_size = 0;
    full_bounded = false;
    is_plain = false;
    current_alignment += padding +
      eprosima::fastcdr::Cdr::alignment(current_alignment, padding);


    last_member_size = 0;
    for (size_t index = 0; index < array_size; ++index) {
      bool inner_full_bounded;
      bool inner_is_plain;
      size_t inner_size =
        deva_navi_msgs::msg::typesupport_fastrtps_cpp::max_serialized_size_Coord3DDoubleProto(
        inner_full_bounded, inner_is_plain, current_alignment);
      last_member_size += inner_size;
      current_alignment += inner_size;
      full_bounded &= inner_full_bounded;
      is_plain &= inner_is_plain;
    }
  }

  // Member: segment_infos
  {
    size_t array_size = 0;
    full_bounded = false;
    is_plain = false;
    current_alignment += padding +
      eprosima::fastcdr::Cdr::alignment(current_alignment, padding);


    last_member_size = 0;
    for (size_t index = 0; index < array_size; ++index) {
      bool inner_full_bounded;
      bool inner_is_plain;
      size_t inner_size =
        deva_navi_msgs::msg::typesupport_fastrtps_cpp::max_serialized_size_SegmentInfoProto(
        inner_full_bounded, inner_is_plain, current_alignment);
      last_member_size += inner_size;
      current_alignment += inner_size;
      full_bounded &= inner_full_bounded;
      is_plain &= inner_is_plain;
    }
  }

  // Member: line_icon_points
  {
    size_t array_size = 0;
    full_bounded = false;
    is_plain = false;
    current_alignment += padding +
      eprosima::fastcdr::Cdr::alignment(current_alignment, padding);


    last_member_size = 0;
    for (size_t index = 0; index < array_size; ++index) {
      bool inner_full_bounded;
      bool inner_is_plain;
      size_t inner_size =
        deva_navi_msgs::msg::typesupport_fastrtps_cpp::max_serialized_size_LineIconPointProto(
        inner_full_bounded, inner_is_plain, current_alignment);
      last_member_size += inner_size;
      current_alignment += inner_size;
      full_bounded &= inner_full_bounded;
      is_plain &= inner_is_plain;
    }
  }

  // Member: city_adcode_list
  {
    size_t array_size = 0;
    full_bounded = false;
    is_plain = false;
    current_alignment += padding +
      eprosima::fastcdr::Cdr::alignment(current_alignment, padding);

    last_member_size = array_size * sizeof(uint64_t);
    current_alignment += array_size * sizeof(uint64_t) +
      eprosima::fastcdr::Cdr::alignment(current_alignment, sizeof(uint64_t));
  }

  // Member: via_point_info
  {
    size_t array_size = 0;
    full_bounded = false;
    is_plain = false;
    current_alignment += padding +
      eprosima::fastcdr::Cdr::alignment(current_alignment, padding);


    last_member_size = 0;
    for (size_t index = 0; index < array_size; ++index) {
      bool inner_full_bounded;
      bool inner_is_plain;
      size_t inner_size =
        deva_navi_msgs::msg::typesupport_fastrtps_cpp::max_serialized_size_ViaPointInfoProto(
        inner_full_bounded, inner_is_plain, current_alignment);
      last_member_size += inner_size;
      current_alignment += inner_size;
      full_bounded &= inner_full_bounded;
      is_plain &= inner_is_plain;
    }
  }

  // Member: charge_station_info
  {
    size_t array_size = 0;
    full_bounded = false;
    is_plain = false;
    current_alignment += padding +
      eprosima::fastcdr::Cdr::alignment(current_alignment, padding);


    last_member_size = 0;
    for (size_t index = 0; index < array_size; ++index) {
      bool inner_full_bounded;
      bool inner_is_plain;
      size_t inner_size =
        deva_navi_msgs::msg::typesupport_fastrtps_cpp::max_serialized_size_ChargeStationInfoProto(
        inner_full_bounded, inner_is_plain, current_alignment);
      last_member_size += inner_size;
      current_alignment += inner_size;
      full_bounded &= inner_full_bounded;
      is_plain &= inner_is_plain;
    }
  }

  // Member: odd_seg_infos
  {
    size_t array_size = 0;
    full_bounded = false;
    is_plain = false;
    current_alignment += padding +
      eprosima::fastcdr::Cdr::alignment(current_alignment, padding);


    last_member_size = 0;
    for (size_t index = 0; index < array_size; ++index) {
      bool inner_full_bounded;
      bool inner_is_plain;
      size_t inner_size =
        deva_navi_msgs::msg::typesupport_fastrtps_cpp::max_serialized_size_OddSegInfoProto(
        inner_full_bounded, inner_is_plain, current_alignment);
      last_member_size += inner_size;
      current_alignment += inner_size;
      full_bounded &= inner_full_bounded;
      is_plain &= inner_is_plain;
    }
  }

  // Member: plan_channel_id
  {
    size_t array_size = 1;

    full_bounded = false;
    is_plain = false;
    for (size_t index = 0; index < array_size; ++index) {
      current_alignment += padding +
        eprosima::fastcdr::Cdr::alignment(current_alignment, padding) +
        1;
    }
  }

  // Member: offline_req_custom_identity_id
  {
    size_t array_size = 1;

    full_bounded = false;
    is_plain = false;
    for (size_t index = 0; index < array_size; ++index) {
      current_alignment += padding +
        eprosima::fastcdr::Cdr::alignment(current_alignment, padding) +
        1;
    }
  }

  // Member: is_valid
  {
    size_t array_size = 1;

    last_member_size = array_size * sizeof(uint8_t);
    current_alignment += array_size * sizeof(uint8_t);
  }

  // Member: path_id
  {
    size_t array_size = 1;

    last_member_size = array_size * sizeof(uint64_t);
    current_alignment += array_size * sizeof(uint64_t) +
      eprosima::fastcdr::Cdr::alignment(current_alignment, sizeof(uint64_t));
  }

  // Member: type
  {
    size_t array_size = 1;

    last_member_size = array_size * sizeof(uint32_t);
    current_alignment += array_size * sizeof(uint32_t) +
      eprosima::fastcdr::Cdr::alignment(current_alignment, sizeof(uint32_t));
  }

  // Member: length
  {
    size_t array_size = 1;

    last_member_size = array_size * sizeof(uint64_t);
    current_alignment += array_size * sizeof(uint64_t) +
      eprosima::fastcdr::Cdr::alignment(current_alignment, sizeof(uint64_t));
  }

  // Member: strategy
  {
    size_t array_size = 1;

    last_member_size = array_size * sizeof(uint64_t);
    current_alignment += array_size * sizeof(uint64_t) +
      eprosima::fastcdr::Cdr::alignment(current_alignment, sizeof(uint64_t));
  }

  // Member: travel_time
  {
    size_t array_size = 1;

    last_member_size = array_size * sizeof(uint64_t);
    current_alignment += array_size * sizeof(uint64_t) +
      eprosima::fastcdr::Cdr::alignment(current_alignment, sizeof(uint64_t));
  }

  // Member: static_travel_time
  {
    size_t array_size = 1;

    last_member_size = array_size * sizeof(uint64_t);
    current_alignment += array_size * sizeof(uint64_t) +
      eprosima::fastcdr::Cdr::alignment(current_alignment, sizeof(uint64_t));
  }

  // Member: toll_cost
  {
    size_t array_size = 1;

    last_member_size = array_size * sizeof(uint32_t);
    current_alignment += array_size * sizeof(uint32_t) +
      eprosima::fastcdr::Cdr::alignment(current_alignment, sizeof(uint32_t));
  }

  // Member: navi_id
  {
    size_t array_size = 1;

    full_bounded = false;
    is_plain = false;
    for (size_t index = 0; index < array_size; ++index) {
      current_alignment += padding +
        eprosima::fastcdr::Cdr::alignment(current_alignment, padding) +
        1;
    }
  }

  // Member: is_online
  {
    size_t array_size = 1;

    last_member_size = array_size * sizeof(uint8_t);
    current_alignment += array_size * sizeof(uint8_t);
  }

  // Member: group_segments
  {
    size_t array_size = 0;
    full_bounded = false;
    is_plain = false;
    current_alignment += padding +
      eprosima::fastcdr::Cdr::alignment(current_alignment, padding);


    last_member_size = 0;
    for (size_t index = 0; index < array_size; ++index) {
      bool inner_full_bounded;
      bool inner_is_plain;
      size_t inner_size =
        deva_navi_msgs::msg::typesupport_fastrtps_cpp::max_serialized_size_GroupSegmentProto(
        inner_full_bounded, inner_is_plain, current_alignment);
      last_member_size += inner_size;
      current_alignment += inner_size;
      full_bounded &= inner_full_bounded;
      is_plain &= inner_is_plain;
    }
  }

  // Member: restriction_info
  {
    size_t array_size = 1;


    last_member_size = 0;
    for (size_t index = 0; index < array_size; ++index) {
      bool inner_full_bounded;
      bool inner_is_plain;
      size_t inner_size =
        deva_navi_msgs::msg::typesupport_fastrtps_cpp::max_serialized_size_RestrictionInfoProto(
        inner_full_bounded, inner_is_plain, current_alignment);
      last_member_size += inner_size;
      current_alignment += inner_size;
      full_bounded &= inner_full_bounded;
      is_plain &= inner_is_plain;
    }
  }

  // Member: end_poi
  {
    size_t array_size = 1;


    last_member_size = 0;
    for (size_t index = 0; index < array_size; ++index) {
      bool inner_full_bounded;
      bool inner_is_plain;
      size_t inner_size =
        deva_navi_msgs::msg::typesupport_fastrtps_cpp::max_serialized_size_POIInfoProto(
        inner_full_bounded, inner_is_plain, current_alignment);
      last_member_size += inner_size;
      current_alignment += inner_size;
      full_bounded &= inner_full_bounded;
      is_plain &= inner_is_plain;
    }
  }

  // Member: tip
  {
    size_t array_size = 1;


    last_member_size = 0;
    for (size_t index = 0; index < array_size; ++index) {
      bool inner_full_bounded;
      bool inner_is_plain;
      size_t inner_size =
        deva_navi_msgs::msg::typesupport_fastrtps_cpp::max_serialized_size_TipInfoProto(
        inner_full_bounded, inner_is_plain, current_alignment);
      last_member_size += inner_size;
      current_alignment += inner_size;
      full_bounded &= inner_full_bounded;
      is_plain &= inner_is_plain;
    }
  }

  // Member: end_direction
  {
    size_t array_size = 1;

    last_member_size = array_size * sizeof(uint32_t);
    current_alignment += array_size * sizeof(uint32_t) +
      eprosima::fastcdr::Cdr::alignment(current_alignment, sizeof(uint32_t));
  }

  // Member: is_holiday_free
  {
    size_t array_size = 1;

    last_member_size = array_size * sizeof(uint8_t);
    current_alignment += array_size * sizeof(uint8_t);
  }

  // Member: is_truck_path
  {
    size_t array_size = 1;

    last_member_size = array_size * sizeof(uint8_t);
    current_alignment += array_size * sizeof(uint8_t);
  }

  // Member: data_version
  {
    size_t array_size = 1;

    last_member_size = array_size * sizeof(uint64_t);
    current_alignment += array_size * sizeof(uint64_t) +
      eprosima::fastcdr::Cdr::alignment(current_alignment, sizeof(uint64_t));
  }

  // Member: normal_plan_time
  {
    size_t array_size = 1;

    last_member_size = array_size * sizeof(uint64_t);
    current_alignment += array_size * sizeof(uint64_t) +
      eprosima::fastcdr::Cdr::alignment(current_alignment, sizeof(uint64_t));
  }

  // Member: route_type
  {
    size_t array_size = 1;

    last_member_size = array_size * sizeof(uint32_t);
    current_alignment += array_size * sizeof(uint32_t) +
      eprosima::fastcdr::Cdr::alignment(current_alignment, sizeof(uint32_t));
  }

  // Member: elec_path_info
  {
    size_t array_size = 1;


    last_member_size = 0;
    for (size_t index = 0; index < array_size; ++index) {
      bool inner_full_bounded;
      bool inner_is_plain;
      size_t inner_size =
        deva_navi_msgs::msg::typesupport_fastrtps_cpp::max_serialized_size_ElecPathInfoProto(
        inner_full_bounded, inner_is_plain, current_alignment);
      last_member_size += inner_size;
      current_alignment += inner_size;
      full_bounded &= inner_full_bounded;
      is_plain &= inner_is_plain;
    }
  }

  // Member: path_description_info
  {
    size_t array_size = 1;


    last_member_size = 0;
    for (size_t index = 0; index < array_size; ++index) {
      bool inner_full_bounded;
      bool inner_is_plain;
      size_t inner_size =
        deva_navi_msgs::msg::typesupport_fastrtps_cpp::max_serialized_size_PathDescriptionInfoProto(
        inner_full_bounded, inner_is_plain, current_alignment);
      last_member_size += inner_size;
      current_alignment += inner_size;
      full_bounded &= inner_full_bounded;
      is_plain &= inner_is_plain;
    }
  }

  // Member: remain_toll_cost
  {
    size_t array_size = 1;

    last_member_size = array_size * sizeof(uint64_t);
    current_alignment += array_size * sizeof(uint64_t) +
      eprosima::fastcdr::Cdr::alignment(current_alignment, sizeof(uint64_t));
  }

  // Member: main_route_remain_toll_cost
  {
    size_t array_size = 1;

    last_member_size = array_size * sizeof(uint64_t);
    current_alignment += array_size * sizeof(uint64_t) +
      eprosima::fastcdr::Cdr::alignment(current_alignment, sizeof(uint64_t));
  }

  size_t ret_val = current_alignment - initial_alignment;
  if (is_plain) {
    // All members are plain, and type is not empty.
    // We still need to check that the in-memory alignment
    // is the same as the CDR mandated alignment.
    using DataType = deva_navi_msgs::msg::AmapPathInfoProto;
    is_plain =
      (
      offsetof(DataType, main_route_remain_toll_cost) +
      last_member_size
      ) == ret_val;
  }

  return ret_val;
}

static bool _AmapPathInfoProto__cdr_serialize(
  const void * untyped_ros_message,
  eprosima::fastcdr::Cdr & cdr)
{
  auto typed_message =
    static_cast<const deva_navi_msgs::msg::AmapPathInfoProto *>(
    untyped_ros_message);
  return cdr_serialize(*typed_message, cdr);
}

static bool _AmapPathInfoProto__cdr_deserialize(
  eprosima::fastcdr::Cdr & cdr,
  void * untyped_ros_message)
{
  auto typed_message =
    static_cast<deva_navi_msgs::msg::AmapPathInfoProto *>(
    untyped_ros_message);
  return cdr_deserialize(cdr, *typed_message);
}

static uint32_t _AmapPathInfoProto__get_serialized_size(
  const void * untyped_ros_message)
{
  auto typed_message =
    static_cast<const deva_navi_msgs::msg::AmapPathInfoProto *>(
    untyped_ros_message);
  return static_cast<uint32_t>(get_serialized_size(*typed_message, 0));
}

static size_t _AmapPathInfoProto__max_serialized_size(char & bounds_info)
{
  bool full_bounded;
  bool is_plain;
  size_t ret_val;

  ret_val = max_serialized_size_AmapPathInfoProto(full_bounded, is_plain, 0);

  bounds_info =
    is_plain ? ROSIDL_TYPESUPPORT_FASTRTPS_PLAIN_TYPE :
    full_bounded ? ROSIDL_TYPESUPPORT_FASTRTPS_BOUNDED_TYPE : ROSIDL_TYPESUPPORT_FASTRTPS_UNBOUNDED_TYPE;
  return ret_val;
}

static message_type_support_callbacks_t _AmapPathInfoProto__callbacks = {
  "deva_navi_msgs::msg",
  "AmapPathInfoProto",
  _AmapPathInfoProto__cdr_serialize,
  _AmapPathInfoProto__cdr_deserialize,
  _AmapPathInfoProto__get_serialized_size,
  _AmapPathInfoProto__max_serialized_size
};

static rosidl_message_type_support_t _AmapPathInfoProto__handle = {
  rosidl_typesupport_fastrtps_cpp::typesupport_identifier,
  &_AmapPathInfoProto__callbacks,
  get_message_typesupport_handle_function,
};

}  // namespace typesupport_fastrtps_cpp

}  // namespace msg

}  // namespace deva_navi_msgs

namespace rosidl_typesupport_fastrtps_cpp
{

template<>
ROSIDL_TYPESUPPORT_FASTRTPS_CPP_EXPORT_deva_navi_msgs
const rosidl_message_type_support_t *
get_message_type_support_handle<deva_navi_msgs::msg::AmapPathInfoProto>()
{
  return &deva_navi_msgs::msg::typesupport_fastrtps_cpp::_AmapPathInfoProto__handle;
}

}  // namespace rosidl_typesupport_fastrtps_cpp

#ifdef __cplusplus
extern "C"
{
#endif

const rosidl_message_type_support_t *
ROSIDL_TYPESUPPORT_INTERFACE__MESSAGE_SYMBOL_NAME(rosidl_typesupport_fastrtps_cpp, deva_navi_msgs, msg, AmapPathInfoProto)() {
  return &deva_navi_msgs::msg::typesupport_fastrtps_cpp::_AmapPathInfoProto__handle;
}

#ifdef __cplusplus
}
#endif
